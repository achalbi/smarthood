$('#community_tabs').html("<%= escape_javascript(render :partial => 'groups/show') %>");
$('#fancybox-loading').remove();
$('.fancybox-overlay').remove();
$(".busy").button('reset');
$('#modal_create_group').modal('hide');
$('#create_grp_form')[0].reset();