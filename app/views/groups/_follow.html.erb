<%= form_for(current_user.user_groups.build(group_id: group.id),
             remote: true) do |f| %>
  <div><%= f.hidden_field :group_id %></div>
  <%= f.submit "Follow", class: "btn primary" %>
<% end %>	