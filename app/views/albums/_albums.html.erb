<% unless @albums.nil? %>
       
       
         <% @albums.each do |album| %>
          <section>
            <h3 style="float:right;"><%= link_to album.title, album, class:"albumLink", remote: true %></h3>
          </section>
         <% end %>
       
 <% end %>  