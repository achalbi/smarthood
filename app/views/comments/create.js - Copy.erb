$("#post_id_"+<%= post.id %>).html("<%= escape_javascript(render('shared/group_feed_item', :group_feed_item => post )) %>")
$("#postId_"+<%= post.id %>).html("<%= escape_javascript(render :partial => 'comments/comment', :locals => { :post => post}) %>")
$('.loading_page').hide();