      <div class="img_zoom" >
       <h4 class="pull-left" style="margin-left:10px;font-size:18px;">
        <div id="albumLink">
          <%= link_to community.name, community, class:"albumLink", id: "cu_name", remote: true %>
        </div>
      </h4>
      <% unless community.photo.nil? %>
      <%= link_to cl_image_tag(community.photo.pic_url(:thumb_medium), class:"roundCorner albumLink", id:"cu_#{community.id}") ,community, class:"albumLink roundCorner", remote: true %>
      <% end %>
    </div>