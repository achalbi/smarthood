$(".busy").button('reset');
$("#posts").html("<%= escape_javascript(render( :partial => 'communities/activity_post', :locals => { :activity => @activity} )) %>");
$("#div_event_post").html("<%= escape_javascript(render( partial: 'posts/post', locals: {posts: @posts, activity: @activity} )) %>");
    	currentPage_post = 1;
      intervalID_post = setInterval(checkScroll_post, 500);
