          <% unless (@inv_req_cu.blank? || @inv_req_cu.count == 0)%>
            <span class="pull-right label important" style="margin:5px;"><%= if @inv_req_cu.count > 0 then @inv_req_cu.count end %></span>
          <% end %>
