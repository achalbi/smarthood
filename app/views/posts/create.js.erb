$(".busy").button('reset');
<% if @post_type == 'activity' %>
$("#posts").html("<%= escape_javascript(render( :partial => 'posts/activity_post', :locals => {:post => @post}, :activity => @activity )) %><div id='div_post' class='resources'><%= escape_javascript(render( partial: 'posts/post', locals: {posts: @posts, activity: @activity} )) %></div><p id='activity_id' class='hide'><%= @activity.id %></p>");
<% end %>

<% if @post_type == 'group' %>
$('.loading_page').hide();
$("#posts").html("<%= escape_javascript(render( :partial => 'posts/group_post', :locals => {:post => @post}, :group => @group )) %><div id='div_post_<%= @group.id.to_s %>' class='resources'><%= escape_javascript(render( partial: 'posts/post', locals: {posts: @posts, group: @group} )) %></div><p id='group_id' class='hide'><%= @group.id %></p>");
<% end %>

<% if @post_type == 'groups' %>
$('#group_list').html("<%= escape_javascript(render :partial => 'posts/groups_post', :locals => {:post => @post}, :groups => @groups) %>");
$('#groups_sec').html("<%= escape_javascript(render :partial => 'groups/group_list', :locals => {:groups => @groups}) %>")

/*var i=0;
$(".bgClrChg2").click( function() {
  var id = $(this).find('p').html();
  if($(this).find(':checkbox').is(':checked')){
    $(this).find(':checkbox').removeAttr("checked");
    $("#grp_check_"+id).removeAttr("checked");
    $("#grp_label_"+id).hide();
    $(this).removeClass('tabs_selected');
    i--;
  }else{
    $(this).find(':checkbox').attr("checked",true);
       $("#grp_check_"+id).attr("checked",true);
       $("#grp_label_"+id).show();
       i++;
       $(this).addClass('tabs_selected');
  }
  if(i==0){
    $("#group_post_submit").hide();
    $("#sel_grp_label").hide();
  }else{
    $("#group_post_submit").show();
    $("#sel_grp_label").show();
  }
});
$("#group_post_submit").hide();
$("#sel_grp_label").hide();*/
<% end %>


<% if @post_type == 'communities' %>
$('#cu_post').html("<%= escape_javascript(render :partial => 'posts/communities_post', :locals => {:post => @post}, :communities => @communities) %>");
<% end %>
<% if @post_type == 'community' %>
$('#community_tabs').html("<%= escape_javascript(render :partial => 'communities/community_post', locals: {posts: @posts, community: @community}) %>");
<% end %>
